<div class="main">
<div class="top_page">

<div class="header">
  <div class="title">Task</div>
  <div class="youkoso"></div>
  <div class="login"></div>
  <div class="logout"></div>
</div>


<div class="task_itiran">

  <div class="kako">
  <div class="kigen_title">期限過ぎ</div>
  <%= form_tag({ controller: :tasks, action: :destroy_multiple }, method: :delete) do %>
    <% @tasks.each do |task| %>
      <% if task.date < Date.today %>
        <div class="task_content">
          <%= check_box_tag 'task_ids[]', task.id %>
          <%= task.date.strftime("%m/%d") %>
          <%= task.title %>
        </div>
      <% end %>
    <% end %>
    <%= submit_tag "完了", class: "task_contents_bottan" %>
  <% end %>
  </div>

  <div class="honzitu">
  <div class="kigen_title">本日のタスク</div>
  <div class="task_contents">
  <%= form_tag({ controller: :tasks, action: :destroy_multiple }, method: :delete) do %>
    <% @tasks.each do |task| %>
      <% if task.date == Date.today %>
        <div class="task_content">
          <%= check_box_tag 'task_ids[]', task.id %>
          <%= task.date.strftime("%m/%d") %>
          <%= task.title %>
        </div>
      <% end %>
    <% end %>
    <%= submit_tag "完了", class: "task_contents_bottan" %>
  <% end %>
  </div>
  </div>

  <div class="mirai">
  <div class="kigen_title">今後のタスク</div>
  <div class="task_contents">
  <%= form_tag({ controller: :tasks, action: :destroy_multiple }, method: :delete) do %>
    <% @tasks.each do |task| %>
      <% if task.date > Date.today %>
        <div class="task_content">
          <%= check_box_tag 'task_ids[]', task.id %>
          <%= task.date.strftime("%m/%d") %>
          <%= task.title %>
        </div>
      <% end %>
    <% end %>
    <%= submit_tag "完了", class: "task_contents_bottan" %>
  <% end %>
  </div>
  </div>

</div>

<div class="footer">
  <div class="task_form">
    <%= form_with model: @task, url: tasks_path, method: :post, local: true do |form| %>
      <%= form.label :date, "日付" %>
      <%= form.date_field :date, value: Date.today.to_s, required: true %>

      <%= form.label :title, "タスク" %>
      <%= form.text_field :title, placeholder: "タスク", required: true %>

      <%= form.submit "投稿" %>
    <% end %>
  </div>
</div>

</div>
</div>
